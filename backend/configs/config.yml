env: "local"

server:
  host: "localhost"
  port: "8000"

db:
  username: "postgres"
  host: "localhost"
  port: "5432"
  dbname: "manager"
  sslmode: "disable"
  password: "postgres"

cors:
  origins: [
    "http://localhost:4200"
  ]
  methods: [
    "GET",
    "POST",
    "PUT",
    "DELETE",
    "HEAD",
    "OPTIONS",
    "PATCH"
  ]
  headers: [
    "*"
  ]